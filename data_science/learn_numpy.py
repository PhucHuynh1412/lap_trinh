import numpy as np 

ma_tran = np.empty(2)
print(ma_tran)

data = []
for num in ma_tran:
    data.append(int(num))

print(data)

maTran = np.arange(10)
print(maTran)

maTran_linspace = np.linspace(1,10,num=5)
print(maTran_linspace)

maTran_so_so = np.linspace (0, 45, num=6)
print(maTran_so_so)

array_example = np.array([[[0, 1, 2, 3],

                           [4, 5, 6, 7]],


                          [[0, 1, 2, 3],

                           [4, 5, 6, 7]],


                          [[0 ,1 ,2, 3],

                           [4, 5, 6, 7]]])
print(array_example)

